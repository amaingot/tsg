steps:
  - name: "gcr.io/cloud-builders/docker"
    entrypoint: "bash"
    args: ["-c", "docker pull gcr.io/hmm-dev/tsg:latest || exit 0"]

  - name: "gcr.io/cloud-builders/docker"
    args: [
        "build",
        "-t",
        "gcr.io/hmm-dev/tsg:latest",
        "--build-arg",
        "COMMIT_SHA=${COMMIT_SHA}",
        "--cache-from=gcr.io/hmm-dev/tsg:latest",
        ".",
      ]

  - name: "gcr.io/cloud-builders/docker"
    args:
      - "tag"
      - "gcr.io/hmm-dev/tsg:latest"
      - "gcr.io/hmm-dev/tsg:$COMMIT_SHA"

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/hmm-dev/tsg:$COMMIT_SHA"]

  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/hmm-dev/tsg:latest"]

  - name: 'gcr.io/cloud-builders/kubectl'
    args:
      - 'set'
      - 'image'
      - 'deployment/tsg'
      - '--namespace=tsg'
      - 'tsg=gcr.io/hmm-dev/tsg:$COMMIT_SHA'
    env:
      - "CLOUDSDK_COMPUTE_ZONE=us-central1-a"
      - "CLOUDSDK_CONTAINER_CLUSTER=hmm"

images:
  - "gcr.io/hmm-dev/tsg:$COMMIT_SHA"
  - "gcr.io/hmm-dev/tsg:latest"
timeout: 720s