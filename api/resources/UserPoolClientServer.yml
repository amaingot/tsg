Type: AWS::Cognito::UserPoolClient
Properties:
  ClientName: ${self:service.app}-UserPoolClient-${self:provider.stage}
  UserPoolId:
    Ref: "UserPool"
  RefreshTokenValidity: 1
  ExplicitAuthFlows:
    - ADMIN_NO_SRP_AUTH
