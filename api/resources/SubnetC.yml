DependsOn: VPC
Type: AWS::EC2::Subnet
Properties:
  VpcId:
    Ref: VPC
  AvailabilityZone: ${self:provider.region}c
  CidrBlock: ${self:provider.environment.VPC_CIDR}.0.2.0/24
  Tags:
    - Key: "Name"
      Value: "${self:service.app}-SubnetC"
